// // This file ensures the crypto global variable is available
// import * as nodeCrypto from 'crypto';

// // Make crypto available globally
// (global as any).crypto = {
//   randomUUID: () => nodeCrypto.randomUUID()
// };
